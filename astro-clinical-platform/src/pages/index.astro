---
/**
 * Astro Clinical Platform - 首頁
 * 專業醫療平台的主要門面，展示核心功能和內容
 */

import BaseLayout from '../layouts/BaseLayout.astro';
import MedicalSEO from '../components/seo/MedicalSEO.astro';
import Search from '../components/islands/Search.tsx';
import { getCollection } from 'astro:content';

// 獲取內容數據
const calculators = await getCollection('calculators');
const education = await getCollection('education');

// 篩選特色內容
const featuredCalculators = calculators
  .filter(calc => calc.data.isFeatured && calc.data.isActive)
  .slice(0, 6);

const featuredEducation = education
  .filter(edu => edu.data.isFeatured && edu.data.isActive)
  .slice(0, 6);

// 統計數據
const stats = {
  calculators: calculators.filter(c => c.data.isActive).length,
  education: education.filter(e => e.data.isActive).length,
  flowcharts: 12, // 暫時使用固定數字
  specialties: [...new Set([
    ...calculators.flatMap(c => c.data.medicalSpecialties || []),
    ...education.flatMap(e => e.data.medicalSpecialties || [])
  ])].length
};

// 頁面設定
const pageTitle = 'Astro Clinical Platform';
const pageDescription = '專為醫療專業人員設計的整合式數位平台，結合臨床決策工具和病患衛教功能，提升醫療效率與品質。';
const locale = 'zh-TW';

// 醫療專科分類
const medicalSpecialties = [
  {
    id: 'cardiology',
    name: '心臟科',
    icon: 'heart',
    color: 'bg-red-500',
    description: '心血管疾病診斷與治療工具'
  },
  {
    id: 'neurology',
    name: '神經科',
    icon: 'brain',
    color: 'bg-purple-500',
    description: '神經系統疾病評估工具'
  },
  {
    id: 'endocrinology',
    name: '內分泌科',
    icon: 'molecule',
    color: 'bg-green-500',
    description: '內分泌代謝疾病管理'
  },
  {
    id: 'general',
    name: '一般醫學',
    icon: 'medical',
    color: 'bg-medical-primary-500',
    description: '基礎醫療評估工具'
  },
  {
    id: 'emergency',
    name: '急診醫學',
    icon: 'emergency',
    color: 'bg-orange-500',
    description: '急診快速評估工具'
  },
  {
    id: 'pediatrics',
    name: '小兒科',
    icon: 'child',
    color: 'bg-pink-500',
    description: '兒童健康評估工具'
  }
];
---

<BaseLayout 
  title={pageTitle}
  description={pageDescription}
  locale={locale}
  pageType="home"
  keywords={['醫療平台', '臨床工具', '病患衛教', '醫療計算機', '診斷流程圖']}
>
  <!-- 醫療 SEO 優化 -->
  <MedicalSEO
    title={pageTitle}
    description={pageDescription}
    contentType="general"
    locale={locale}
    medicalSpecialty={['general', 'internal-medicine']}
    patientFriendly={false}
    slot="head"
  />

  <!-- 主要內容區域 -->
  <div class="min-h-screen">
    <!-- Hero Section -->
    <section class="bg-gradient-to-br from-medical-primary-600 via-medical-primary-700 to-medical-primary-800 text-white">
      <div class="container mx-auto px-4 py-16 lg:py-24">
        <div class="max-w-4xl mx-auto text-center">
          <!-- 主標題 -->
          <h1 class="text-4xl lg:text-6xl font-bold mb-6 leading-tight">
            <span class="block">專業醫療</span>
            <span class="block text-medical-accent-300">數位平台</span>
          </h1>
          
          <!-- 副標題 -->
          <p class="text-xl lg:text-2xl mb-8 text-white leading-relaxed">
            整合臨床決策工具與病患衛教資源<br>
            提升醫療效率，優化診療品質
          </p>
          
          <!-- 搜尋框 -->
          <div class="max-w-2xl mx-auto mb-8">
            <Search 
              client:load
              locale={locale}
              placeholder="搜尋醫療工具和衛教內容"
              className="w-full"
            />
          </div>
          
          <!-- 快速導覽按鈕 -->
          <div class="flex flex-wrap justify-center gap-4">
            <a 
              href="/tools" 
              class="bg-white text-medical-primary-700 px-8 py-3 rounded-lg font-semibold hover:bg-medical-primary-50 transition-colors shadow-lg"
            >
              醫療工具
            </a>
            <a 
              href="/education" 
              class="bg-medical-primary-500 text-white px-8 py-3 rounded-lg font-semibold hover:bg-medical-primary-400 transition-colors shadow-lg border border-medical-primary-400"
            >
              衛教資源
            </a>
            <a 
              href="/categories" 
              class="bg-transparent text-white px-8 py-3 rounded-lg font-semibold hover:bg-medical-primary-600 transition-colors border border-white"
            >
              專科分類
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- 統計數據 -->
    <section class="py-16 bg-white">
      <div class="container mx-auto px-4">
        <div class="grid grid-cols-2 lg:grid-cols-4 gap-8">
          <div class="text-center">
            <div class="text-4xl lg:text-5xl font-bold text-medical-primary-600 mb-2">
              {stats.calculators}
            </div>
            <div class="text-gray-700 font-semibold">醫療計算機</div>
          </div>
          <div class="text-center">
            <div class="text-4xl lg:text-5xl font-bold text-medical-primary-600 mb-2">
              {stats.education}
            </div>
            <div class="text-gray-700 font-semibold">衛教文章</div>
          </div>
          <div class="text-center">
            <div class="text-4xl lg:text-5xl font-bold text-medical-primary-600 mb-2">
              {stats.flowcharts}
            </div>
            <div class="text-gray-700 font-semibold">診斷流程圖</div>
          </div>
          <div class="text-center">
            <div class="text-4xl lg:text-5xl font-bold text-medical-primary-600 mb-2">
              {stats.specialties}
            </div>
            <div class="text-gray-700 font-semibold">醫療專科</div>
          </div>
        </div>
      </div>
    </section>

    <!-- 醫療專科分類 -->
    <section class="py-16 bg-gray-50">
      <div class="container mx-auto px-4">
        <div class="text-center mb-12">
          <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-4">
            醫療專科分類
          </h2>
          <p class="text-xl text-gray-700 max-w-3xl mx-auto">
            按照醫療專科瀏覽相關的臨床工具和衛教內容
          </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {medicalSpecialties.map((specialty) => (
            <a 
              href={`/specialties/${specialty.id}`}
              class="bg-white rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 hover:-translate-y-1 group"
            >
              <div class="flex items-center mb-4">
                <div class={`w-12 h-12 ${specialty.color} rounded-lg flex items-center justify-center mr-4 group-hover:scale-110 transition-transform`}>
                  <svg class="w-6 h-6 text-white" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
                  </svg>
                </div>
                <h3 class="text-xl font-semibold text-gray-900 group-hover:text-medical-primary-600 transition-colors">
                  {specialty.name}
                </h3>
              </div>
              <p class="text-gray-700">
                {specialty.description}
              </p>
            </a>
          ))}
        </div>
      </div>
    </section>

    <!-- 特色醫療工具 -->
    <section class="py-16 bg-white">
      <div class="container mx-auto px-4">
        <div class="text-center mb-12">
          <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-4">
            特色醫療工具
          </h2>
          <p class="text-xl text-gray-700 max-w-3xl mx-auto">
            精選的臨床決策工具，協助醫療專業人員進行快速且準確的評估
          </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
          {featuredCalculators.map((calculator) => (
            <div class="bg-white rounded-xl border border-gray-200 p-6 hover:shadow-lg transition-shadow">
              <div class="flex items-start justify-between mb-4">
                <div class="flex-1">
                  <h3 class="text-lg font-semibold text-gray-900 mb-2">
                    {calculator.data.name['zh-TW']}
                  </h3>
                  <p class="text-gray-700 text-sm leading-relaxed">
                    {calculator.data.description['zh-TW']}
                  </p>
                </div>
                <span class={`inline-flex items-center px-2 py-1 rounded-full text-xs font-medium ${
                  calculator.data.category === 'cardiology' ? 'bg-red-100 text-red-800' :
                  calculator.data.category === 'neurology' ? 'bg-purple-100 text-purple-800' :
                  calculator.data.category === 'endocrinology' ? 'bg-green-100 text-green-800' :
                  'bg-medical-primary-100 text-medical-primary-800'
                }`}>
                  {calculator.data.category === 'cardiology' ? '心臟科' :
                   calculator.data.category === 'neurology' ? '神經科' :
                   calculator.data.category === 'endocrinology' ? '內分泌科' :
                   '一般醫學'}
                </span>
              </div>
              
              <div class="flex items-center justify-between">
                <div class="flex items-center space-x-2 text-sm text-gray-500">
                  <span class={`inline-flex items-center px-2 py-1 rounded text-xs ${
                    calculator.data.difficulty === 'basic' ? 'bg-green-100 text-green-700' :
                    calculator.data.difficulty === 'intermediate' ? 'bg-yellow-100 text-yellow-700' :
                    'bg-red-100 text-red-700'
                  }`}>
                    {calculator.data.difficulty === 'basic' ? '基礎' :
                     calculator.data.difficulty === 'intermediate' ? '中級' : '進階'}
                  </span>
                  <span class="text-xs text-gray-400">
                    證據等級: {calculator.data.evidenceLevel}
                  </span>
                </div>
                <a 
                  href={`/tools/${calculator.data.id}`}
                  class="inline-flex items-center text-medical-primary-600 hover:text-medical-primary-700 font-medium text-sm"
                >
                  使用工具
                  <svg class="ml-1 w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                  </svg>
                </a>
              </div>
            </div>
          ))}
        </div>
        
        <div class="text-center">
          <a 
            href="/tools" 
            class="inline-flex items-center bg-medical-primary-600 text-white px-8 py-3 rounded-lg font-semibold hover:bg-medical-primary-700 transition-colors"
          >
            查看所有工具
            <svg class="ml-2 w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </a>
        </div>
      </div>
    </section>

    <!-- 衛教資源 -->
    <section class="py-16 bg-gray-50">
      <div class="container mx-auto px-4">
        <div class="text-center mb-12">
          <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-4">
            衛教資源
          </h2>
          <p class="text-xl text-gray-700 max-w-3xl mx-auto">
            專業的醫療衛教內容，協助醫師與患者進行有效溝通
          </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
          {featuredEducation.map((article) => (
            <article class="bg-white rounded-xl border border-gray-200 overflow-hidden hover:shadow-lg transition-shadow">
              {article.data.featuredImage && (
                <div class="aspect-video bg-gray-200 relative overflow-hidden">
                  <img 
                    src={article.data.featuredImage.src}
                    alt={article.data.featuredImage.alt['zh-TW']}
                    class="w-full h-full object-cover hover:scale-105 transition-transform duration-300"
                  />
                </div>
              )}
              
              <div class="p-6">
                <div class="flex items-center space-x-2 mb-3">
                  <span class={`inline-flex items-center px-2 py-1 rounded-full text-xs font-medium ${
                    article.data.category === 'disease' ? 'bg-red-100 text-red-800' :
                    article.data.category === 'medication' ? 'bg-medical-primary-100 text-medical-primary-800' :
                    article.data.category === 'procedure' ? 'bg-purple-100 text-purple-800' :
                    'bg-green-100 text-green-800'
                  }`}>
                    {article.data.category === 'disease' ? '疾病資訊' :
                     article.data.category === 'medication' ? '藥物資訊' :
                     article.data.category === 'procedure' ? '檢查程序' :
                     '預防保健'}
                  </span>
                  {article.data.readingTime && (
                    <span class="text-xs text-gray-500">
                      {article.data.readingTime} 分鐘閱讀
                    </span>
                  )}
                </div>
                
                <h3 class="text-lg font-semibold text-gray-900 mb-2 line-clamp-2">
                  {article.data.title['zh-TW']}
                </h3>
                
                {article.data.excerpt && (
                  <p class="text-gray-700 text-sm leading-relaxed mb-4 line-clamp-3">
                    {article.data.excerpt['zh-TW']}
                  </p>
                )}
                
                <div class="flex items-center justify-between">
                  <div class="flex items-center space-x-2">
                    {article.data.hasFlowchart && (
                      <span class="inline-flex items-center text-xs text-medical-primary-600">
                        <svg class="w-3 h-3 mr-1" fill="currentColor" viewBox="0 0 24 24">
                          <path d="M13 10V3L4 14h7v7l9-11h-7z"/>
                        </svg>
                        流程圖
                      </span>
                    )}
                    <span class={`text-xs ${
                      article.data.difficulty === 'basic' ? 'text-green-600' :
                      article.data.difficulty === 'intermediate' ? 'text-yellow-600' :
                      'text-red-600'
                    }`}>
                      {article.data.difficulty === 'basic' ? '基礎' :
                       article.data.difficulty === 'intermediate' ? '中級' : '進階'}
                    </span>
                  </div>
                  
                  <a 
                    href={`/education/${article.id}`}
                    class="inline-flex items-center text-medical-primary-600 hover:text-medical-primary-700 font-medium text-sm"
                  >
                    閱讀更多
                    <svg class="ml-1 w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
                    </svg>
                  </a>
                </div>
              </div>
            </article>
          ))}
        </div>
        
        <div class="text-center">
          <a 
            href="/education" 
            class="inline-flex items-center bg-medical-primary-600 text-white px-8 py-3 rounded-lg font-semibold hover:bg-medical-primary-700 transition-colors"
          >
            查看所有衛教內容
            <svg class="ml-2 w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3" />
            </svg>
          </a>
        </div>
      </div>
    </section>

    <!-- 演示功能 -->
    <section class="py-16 bg-medical-neutral-50">
      <div class="container mx-auto px-4">
        <div class="text-center mb-12">
          <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-4">
            功能演示
          </h2>
          <p class="text-xl text-gray-700 max-w-3xl mx-auto">
            體驗平台的核心功能和互動式工具
          </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          <div class="bg-white rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 hover:-translate-y-1">
            <div class="w-12 h-12 bg-medical-primary-100 rounded-lg flex items-center justify-center mb-4">
              <svg class="w-6 h-6 text-medical-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"></path>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 mb-3">藥物資訊系統</h3>
            <p class="text-gray-700 mb-4">
              互動式藥物查詢、比較和詳細資訊展示系統，支援多語言介面和智能搜尋功能。
            </p>
            <a 
              href="/medication-system-demo" 
              class="inline-flex items-center text-medical-primary-600 hover:text-medical-primary-700 font-medium"
            >
              體驗演示
              <svg class="ml-1 w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </a>
          </div>

          <div class="bg-white rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 hover:-translate-y-1">
            <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center mb-4">
              <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 mb-3">醫療圖表系統</h3>
            <p class="text-gray-700 mb-4">
              動態醫療數據視覺化，包含生命徵象監控、趨勢分析和互動式圖表展示。
            </p>
            <a 
              href="/medical-charts-demo" 
              class="inline-flex items-center text-green-600 hover:text-green-700 font-medium"
            >
              體驗演示
              <svg class="ml-1 w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </a>
          </div>

          <div class="bg-white rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 hover:-translate-y-1">
            <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center mb-4">
              <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 8a2 2 0 012-2h10a2 2 0 012 2v10a2 2 0 01-2 2H7a2 2 0 01-2-2V8z M9 12l2 2 4-4"></path>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 mb-3">臨床決策樹</h3>
            <p class="text-gray-700 mb-4">
              互動式臨床決策支援工具，引導醫療專業人員進行系統性診斷和治療決策。
            </p>
            <a 
              href="/decision-trees" 
              class="inline-flex items-center text-purple-600 hover:text-purple-700 font-medium"
            >
              體驗演示
              <svg class="ml-1 w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </a>
          </div>

          <div class="bg-white rounded-xl p-6 shadow-lg hover:shadow-xl transition-all duration-300 hover:-translate-y-1">
            <div class="w-12 h-12 bg-orange-100 rounded-lg flex items-center justify-center mb-4">
              <svg class="w-6 h-6 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
              </svg>
            </div>
            <h3 class="text-xl font-semibold text-gray-900 mb-3">程序說明系統</h3>
            <p class="text-gray-700 mb-4">
              詳細的醫療程序說明，包含準備工作、風險評估、程序步驟和術後護理指導。
            </p>
            <a 
              href="/procedure-explanation-demo" 
              class="inline-flex items-center text-orange-600 hover:text-orange-700 font-medium"
            >
              體驗演示
              <svg class="ml-1 w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- 平台特色 -->
    <section class="py-16 bg-white">
      <div class="container mx-auto px-4">
        <div class="text-center mb-12">
          <h2 class="text-3xl lg:text-4xl font-bold text-gray-900 mb-4">
            平台特色
          </h2>
          <p class="text-xl text-gray-700 max-w-3xl mx-auto">
            專為醫療專業人員設計的功能特色
          </p>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
          <div class="text-center">
            <div class="w-16 h-16 bg-medical-primary-100 rounded-full flex items-center justify-center mx-auto mb-4">
              <svg class="w-8 h-8 text-medical-primary-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 7h6m0 10v-3m-3 3h.01M9 17h.01M9 14h.01M12 14h.01M15 11h.01M12 11h.01M9 11h.01M7 21h10a2 2 0 002-2V5a2 2 0 00-2-2H7a2 2 0 00-2 2v14a2 2 0 002 2z" />
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-2">精準計算</h3>
            <p class="text-gray-700 text-sm">基於最新臨床指引的醫療計算工具</p>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 bg-medical-primary-100 rounded-full flex items-center justify-center mx-auto mb-4">
              <svg class="w-8 h-8 text-medical-primary-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-2">快速搜尋</h3>
            <p class="text-gray-700 text-sm">3秒內找到所需的醫療工具和資訊</p>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 bg-medical-primary-100 rounded-full flex items-center justify-center mx-auto mb-4">
              <svg class="w-8 h-8 text-medical-primary-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.746 0 3.332.477 4.5 1.253v13C19.832 18.477 18.246 18 16.5 18c-1.746 0-3.332.477-4.5 1.253" />
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-2">專業內容</h3>
            <p class="text-gray-700 text-sm">經醫療專家審核的高品質衛教內容</p>
          </div>
          
          <div class="text-center">
            <div class="w-16 h-16 bg-medical-primary-100 rounded-full flex items-center justify-center mx-auto mb-4">
              <svg class="w-8 h-8 text-medical-primary-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z" />
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-2">視覺化流程</h3>
            <p class="text-gray-700 text-sm">互動式診斷和治療流程圖</p>
          </div>
        </div>
      </div>
    </section>

    <!-- 醫療免責聲明 -->
    <section class="py-12 bg-medical-warning-50 border-t border-medical-warning-200">
      <div class="container mx-auto px-4">
        <div class="max-w-4xl mx-auto">
          <div class="flex items-start space-x-4">
            <div class="flex-shrink-0">
              <svg class="w-6 h-6 text-medical-warning-600 mt-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-2.5L13.732 4c-.77-.833-1.964-.833-2.732 0L3.732 16.5c-.77.833.192 2.5 1.732 2.5z" />
              </svg>
            </div>
            <div>
              <h3 class="text-lg font-semibold text-medical-warning-800 mb-2">醫療免責聲明</h3>
              <p class="text-medical-warning-700 leading-relaxed">
                本平台提供的醫療資訊和工具僅供教育和參考用途，不能取代專業醫療建議、診斷或治療。
                所有醫療決策應基於醫療專業人員的臨床判斷，並考慮患者的個別情況。
                如有任何健康問題或疑慮，請諮詢合格的醫療專業人員。
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>
  </div>
</BaseLayout>

<style>
  /* 自定義樣式 */
  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  
  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  
  /* 漸層背景動畫 */
  .bg-gradient-to-br {
    background-size: 400% 400%;
    animation: gradientShift 15s ease infinite;
  }
  
  @keyframes gradientShift {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }
  
  /* 卡片懸停效果 */
  .hover\:-translate-y-1:hover {
    transform: translateY(-0.25rem);
  }
  
  /* 響應式設計增強 */
  @media (max-width: 768px) {
    .container {
      padding-left: 1rem;
      padding-right: 1rem;
    }
  }
  
  /* 無障礙增強 */
  @media (prefers-reduced-motion: reduce) {
    .bg-gradient-to-br {
      animation: none;
    }
    
    .hover\:-translate-y-1:hover {
      transform: none;
    }
    
    .group-hover\:scale-110:hover {
      transform: none;
    }
  }
  
  /* 高對比模式支援 */
  @media (prefers-contrast: high) {
    .bg-gradient-to-br {
      background: #1e40af;
    }
    
    .text-gray-600 {
      color: #374151;
    }
    
    .border-gray-200 {
      border-color: #6b7280;
    }
  }
  
  /* 列印樣式 */
  @media print {
    .bg-gradient-to-br {
      background: #1e40af !important;
      color: white !important;
    }
    
    .shadow-lg,
    .shadow-xl {
      box-shadow: none !important;
      border: 1px solid #d1d5db !important;
    }
  }
</style>
